<%
function addStyles(list) {
  if (Array.isArray(list) === true) {
    list.forEach(function(elm, i, ary) {
-%>
  <link rel="stylesheet" href="<%= CSS_PATH %><%= elm %>">
<%
    });
  }
}
addStyles(addStylesList);
-%>

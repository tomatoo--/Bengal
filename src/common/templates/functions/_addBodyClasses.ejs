<%
var classes = [];
function addBodyClasses(list) {
  if (Array.isArray(list) === true) {
    list.forEach(function(elm, i, ary) {
      classes.push(elm);
    });
    return classes.join(' ');
  }
}
-%>
<%= addBodyClasses(addBodyClassesList); -%>
